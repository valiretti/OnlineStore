@model IEnumerable<OnlineStore.Web.Models.CategoryViewModel>
@{
    ViewBag.Title = "Get Categories";
}

<table class="table table-borderless">
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(item.Name, "GetCertainCategoryProducts", new { id = item.Id })
                @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
                {
                    @Html.ActionLink(" | Delete", "DeleteCategory", new { id = item.Id })
                }

                @{
                    Html.RenderAction("GetCompaniesForCategory", "Home", new { categoryId = item.Id });
                }
            </td>
        </tr>
    }
</table>











